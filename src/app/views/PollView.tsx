import * as m from 'mithril';

export class PollView implements m.Component {
    view() {
        return (
            <div className="container-fluid">
                Holla holla bits
            </div>
        )
    }
}